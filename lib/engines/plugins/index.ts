/**
 * 引擎插件注册表（自动维护）
 * 使用 new-engine.mjs 脚本会自动更新此文件
 */

// 这里会自动添加引擎导出
// 例如：export { default as bt1207 } from './bt1207';
// 例如：export { default as btdig } from './btdig';

// 暂时为空，使用 scripts/new-engine.mjs 添加新引擎
